<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Projeto Literário</title>

  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display&family=Crimson+Text&display=swap" rel="stylesheet">

  <style>
    body {
      background: #0f1115;
      color: #e6e1d8;
      font-family: "Crimson Text", serif;
      margin: 0;
    }

    header, footer {
      text-align: center;
      padding: 30px 20px;
      border-bottom: 1px solid #2a2d34;
    }

    nav a {
      color: #c8b79a;
      margin: 0 10px;
      text-decoration: none;
      font-style: italic;
    }

    main {
      max-width: 700px;
      margin: 60px auto;
      padding: 0 20px;
    }

    .box {
      background: #161922;
      padding: 40px;
      border-left: 4px solid #8b6f47;
    }

    .data {
      font-style: italic;
      color: #9c9487;
      margin-bottom: 20px;
    }

    a {
      color: #c8b79a;
    }
  </style>
</head>
<body>

<header>
  <h1>Projeto Literário</h1>
  <nav>
    <a href="index.html">Início</a>
    <a href="registro.html">Registro de Temporadas</a>
  </nav>
</header>

<main id="conteudo"></main>

<footer>
  © 2026
</footer>

<script>
  const temporada = {
    nome: "Temporada I — Fragmentos",
    inicio: "2026-03-01",
    duracao: 10,
    textos: [
      "Poema 1.",
      "Poema 2.",
      "Poema 3.",
      "Poema 4.",
      "Poema 5.",
      "Poema 6.",
      "Poema 7.",
      "Poema 8.",
      "Poema 9.",
      "Poema 10."
    ]
  };

  const inicio = new Date(temporada.inicio);
  const hoje = new Date();
  const diffDias = Math.floor((hoje - inicio) / 86400000);

  const container = document.getElementById("conteudo");

  if (diffDias >= 0 && diffDias < temporada.duracao) {
    const seed = 12345;
    function shuffle(arr) {
      let s = seed;
      function rand() {
        s = (s * 16807) % 2147483647;
        return (s - 1) / 2147483646;
      }
      for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(rand() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
      return arr;
    }

    const ordem = shuffle([...Array(temporada.textos.length).keys()]);
    const textoHoje = temporada.textos[ordem[diffDias]];

    container.innerHTML = `
      <div class="box">
        <h2>${temporada.nome}</h2>
        <div class="data">${hoje.toLocaleDateString("pt-BR")}</div>
        <p>${textoHoje}</p>
      </div>
    `;
  } else if (diffDias >= temporada.duracao) {
    container.innerHTML = `
      <div class="box">
        <h2>${temporada.nome} encerrada</h2>
        <p>
          <a href="temporadas/temporada-1.html">
            Ler todos os poemas da temporada
          </a>
        </p>
      </div>
    `;
  }
</script>

</body>
</html>      max-width: 720px;
      margin: 60px auto;
      padding: 0 20px;
    }

    .texto-dia {
      background: #161922;
      padding: 40px;
      border-left: 4px solid #8b6f47;
      box-shadow: 0 0 20px rgba(0,0,0,0.4);
    }

    .texto-dia h2 {
      font-family: "Playfair Display", serif;
      font-weight: 400;
      margin-top: 0;
      color: #d6c9b5;
      letter-spacing: 0.5px;
    }

    .data {
      font-size: 0.9em;
      color: #9c9487;
      margin-bottom: 25px;
      font-style: italic;
    }

    .texto-dia p {
      font-size: 1.15em;
    }

    footer {
      text-align: center;
      padding: 30px 20px;
      font-size: 0.85em;
      color: #777064;
      border-top: 1px solid #2a2d34;
    }
  </style>
</head>
<body>

  <header>
    <h1>Sua Dose Diária de Poesia</h1>
  </header>

  <main></main>

  <footer>
    © 2026 Todos os direitos reservados.
  </footer>

  <script>
    // ✒️ Textos literários (adicione quantos quiser)
    const
    textos = [
      "Quando eu estava com voce, implorava ao Tempo para que ele andasse devagar. Agora, eu insisto e clamo para que Ele corra o mais rápido possível. Porque sem voce, nao há sentido.",
      "Ontem eu estava cheio. Hoje estou vazio",
      "A lembranca do nao-dito, do nao-feito, do nao-amado. Oh! É tao terrível. ",
      "Eu to doente. Já fazem dias que eu tenho os sintomas... Nao consigo parar de pensar em voce, Nao me esqueco dos dias em que estávamos juntos E até mesmo nos meus sonhos voce veio me procurar... Eu to doente de amor por voce.",
      "Eu escrevo para fugir. Eu escrevo para fingir. Eu escrevo para nao ter que enfrentar o mundo. Eu escrevo para poder entrar num lugar onde nao preciso ter vergonha. Eu escrevo para poder ser eu mesmo. Eu escrevo para poder mudar o mundo."
    ];

    // Data base fixa (não mude depois de publicar)
    const dataBase = new Date(2024, 0, 1);

    // Gera uma ordem embaralhada SEM repetir (seed fixa)
    function gerarOrdemEmbaralhada(total) {
      const indices = [...Array(total).keys()];

      let seed = 123456; // seed fixa = mesma ordem para todos
      function random() {
        seed = (seed * 16807) % 2147483647;
        return (seed - 1) / 2147483646;
      }

      for (let i = indices.length - 1; i > 0; i--) {
        const j = Math.floor(random() * (i + 1));
        [indices[i], indices[j]] = [indices[j], indices[i]];
      }

      return indices;
    }

    const ordem = gerarOrdemEmbaralhada(textos.length);

    // Calcula o dia atual relativo à data base
    const hoje = new Date();
    const diffDias = Math.floor(
      (hoje - dataBase) / (1000 * 60 * 60 * 24)
    );

    const indiceHoje = ordem[diffDias % textos.length];
    const textoHoje = textos[indiceHoje];

    // Formata data bonita
    const dataFormatada = hoje.toLocaleDateString("pt-BR", {
      day: "2-digit",
      month: "long",
      year: "numeric"
    });

    const main = document.querySelector("main");

    const box = document.createElement("div");
    box.className = "texto-dia";
    box.innerHTML = `
      <h2>Poema do Dia</h2>
      <div class="data">${dataFormatada}</div>
      <p>${textoHoje}</p>
    `;

    main.appendChild(box);
  </script>

</body>
</html>
